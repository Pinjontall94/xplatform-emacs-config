;;   ===============================================================
;;  == Miscellaneous (Stuff I don't want to clog up the init.el) ==
;; ===============================================================

;; show the readme in dired buffers
(use-package dired-auto-readme :ensure t)

(provide 'misc-conf)
